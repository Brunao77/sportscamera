---
const { placeholder, type, required, minlength, name  } = Astro.props;
---

<div class="relative">
	<slot />
	<input type={type} minlength={minlength} name={name} required={required} class="bg-white border-primary border-2 outline-0 px-10 py-3 text-terciary rounded-xl placeholder-terciary disabled:border-primary_disabled" placeholder={placeholder}></input>
</div>
